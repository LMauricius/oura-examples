import (ABIMod, CatchStack, CatchLookUp) module ABI Core

:Exceptional(E $Any) => proto ABIMod((storage = CatchStack), E)

:Unexpected(E $Any) => proto ABIMod((storage = CatchLookUp), E)

DivByZero = Exceptional.class

:divided(a $ArgTrait, b $ArgTrait, ArgTrait $proto Number) -> ArgT|DivByZero => {
    if b ?= 0 as ArgT {
        return DivByZero
    } else {
        return a / b
    }
}