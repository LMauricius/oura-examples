use (ABIMod, CatchStack, CatchLookUp) module ABI Core

Exceptional(E $Any) => proto ABIMod((storage = CatchStack), E)

Unexpected(E $Any) => proto ABIMod((storage = CatchLookUp), E)

DivByZero = Exceptional'class

divided(a $ArgTrait, b $ArgTrait, ArgTrait $proto Number, Zero=ArgTrait'0) => (ArgTrait|DivByZero)'{
    if b ?= Zero {
        return DivByZero
    } else {
        return a / b
    }
}